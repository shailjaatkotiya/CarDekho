{
  "name": "cardekho-replica-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend-only car discovery MVP powered by local JSON data",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm --prefix frontend run dev -- --host 127.0.0.1",
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix frontend run test"
  }
}
